version: 2
jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
      - run: npm install
      - run: ./gradlew gulpTests

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
